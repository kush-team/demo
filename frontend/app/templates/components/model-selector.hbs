{{#if data}}
{{view "select"
       content=data
       optionLabelPath=optionLabelPath
       selection=value
       class='ui dropdown tiny'
       value=value
       prompt='Selecciona una opción'
       }}
{{/if}}