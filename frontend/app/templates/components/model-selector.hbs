{{#if data}}
{{view "select"
       content=data
       optionLabelPath='content.name'
       selection=value
       class='ui dropdown tiny'
       value=value
       prompt='Selecciona una opcion'
       }}
{{/if}}